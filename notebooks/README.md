# Notebooks

This directory is for labelled exploration only. Production logic must live in `src/dlm/`, be
called by the CLI, and be covered by tests. A notebook may illustrate an experiment but can never
be the sole source of a result used in the report.
